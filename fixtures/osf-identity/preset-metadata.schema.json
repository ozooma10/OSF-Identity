{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:osf-identity:preset-metadata:v1",
  "title": "OSF Identity preset dependency metadata",
  "description": "Optional <localFormId>.json sidecar next to <localFormId>.npc inside an owning-plugin directory. Omitted requires defaults to an empty set; a malformed sidecar disables that preset alone.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "schemaVersion": {
      "const": 1
    },
    "requires": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "plugins",
        "assets"
      ],
      "properties": {
        "plugins": {
          "type": "array",
          "maxItems": 256,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^[^\\\\/:]+\\.[eE][sS][mMpPlL]$"
          }
        },
        "assets": {
          "type": "array",
          "maxItems": 256,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    }
  }
}
