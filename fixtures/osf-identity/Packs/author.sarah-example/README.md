# Example preset slot

Copy a producer-round-tripped `Sarah.npc` export here before installing this
example as a pack. This source directory intentionally contains no active or
fabricated preset payload.

The explicit `Starfield.esm:00005983` target is load-order-safe and does not
depend on vanilla NPC EditorIDs being present in the runtime lookup table.
